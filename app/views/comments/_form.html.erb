<% if current_user %>
  <h4>Add a comment:</h4>
  <%= form_for [post, comment] do |f| %>
    <%= f.text_area :body, row: 2, class: 'form_control', placeholder: 'Type your comments here...' %>
    <%= f.submit "Submit" %>
  <% end %>
<% end %>

